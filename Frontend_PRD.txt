
Each step includes: *goal*, *acceptance criteria*, and a **Cursor prompt** to scaffold/automate the work. Run steps one by one.

* Insert test data if applicable.
* Run unit/integration/E2E tests for this step's feature.
* Fix any errors or failing tests.
* Re-run tests to confirm all pass before moving to next step.

**Preparation & Repo**

1. Initialize monorepo & backend project

   * Goal: Create repo, setup Node/TypeScript, ESLint, Prettier, package.json
   * Acceptance: `yarn dev` runs a stub server
   * Cursor prompt:

     ```text
     Create a new TypeScript Node project named "multi-tenant-backend" with Express, TypeScript, ESLint, Prettier, and common npm scripts (dev, build, start, test). Initialize git and create basic README.
     ```

2. Add Prisma & MySQL connection for DB `multi-tenant-scale` and create initial Prisma config

   * Goal: prisma init, datasource set to MySQL env var
   * Acceptance: `prisma generate` succeeds
   * Cursor prompt:

     ```text
     Add Prisma to the project and configure it for MySQL. Set datasource url to process.env.DATABASE_URL and database name multi-tenant-scale. Generate prisma client.
     ```

3. Create base `.env` templates and Zod-based env loader

   * Goal: Add `.env.example` and environment loader using Zod to validate required env vars
   * Acceptance: app errors if env missing
   * Cursor prompt:

     ```text
     Create .env.example with DB, JWT keys path, PORT, and NODE_ENV. Implement a Zod-based env loader in src/config/env.ts that throws when required variables are missing.
     ```

4. Add JWT key folder & initial keys (gitignored)

   * Goal: Place `jwt-keys/private.key`, `public.key`, `secret.key` and reference them in env
   * Acceptance: auth helper reads keys
   * Cursor prompt:

     ```text
     Add jwt-keys folder and generate RSA private/public keys and a fallback HMAC secret key. Add reading logic in src/helpers/jwt.ts to load keys from ENV paths.
     ```

* Push this code to GitHub after testing with title "Preparation & Repo dynamic" as per the title.

* Insert test data if applicable.
* Run unit/integration/E2E tests for this step's feature.
* Fix any errors or failing tests.
* Re-run tests to confirm all pass before moving to next step.

**Database Schema & Migrations**


5. Design Prisma schema / SQL create tables for tenants, users, roles, modules, role\_permissions, support, audit, tokens, login\_devices

   * Goal: Full schema created, indexed
   * Acceptance: prisma migrate dev creates DB
   * Cursor prompt:

     ```text
     Create a Prisma schema that defines tenants, tenant_settings, users (with is_superadmin), roles, modules (parent_id), submodules, role_permissions, user_roles, support_tickets, support_replies, support_attachments, audit_logs, refresh_tokens, reset_tokens, login_devices, system_settings. Include indexes and FK constraints. Run prisma migrate dev.
     ```

6. Seed initial data (modules, default roles, SuperAdmin user, two tenants)

   * Goal: seed.ts populates DB with sample data
   * Acceptance: sample data present
   * Cursor prompt:

     ```text
     Create prisma/seed.ts that inserts modules (dashboard, users, roles, support, notifications), default global roles (superadmin, global_support), two tenants (tenantA, tenantB), a superadmin user and sample tenant admin and users. Run the seed via prisma.
     ```

* Push this code to GitHub after testing with title "Database Schema & Migrations dynamic" as per the title.

* Insert test data if applicable.
* Run unit/integration/E2E tests for this step's feature.
* Fix any errors or failing tests.
* Re-run tests to confirm all pass before moving to next step.

**Core Services & Repositories**

7. Implement Prisma client wrapper & repository pattern

   * Goal: Single exported Prisma client instance and base repository
   * Acceptance: Services import repositories
   * Cursor prompt:

     ```text
     Add src/repositories/prisma.ts with exported Prisma client and create base repository files for users, tenants, roles, modules, support, tokens, audit.
     ```

8. Implement user service (create, find, update, soft delete)

   * Goal: CRUD user service with password hashing
   * Acceptance: unit tests for user service
   * Cursor prompt:

     ```text
     Implement UserService with functions createUser, getUserById, listUsers (filters/pagination/search), updateUser, softDeleteUser. Use bcrypt for hashing and Zod for input validation.
     ```

9. Implement tenant service & tenant settings

   * Goal: tenant CRUD and tenant\_settings merge with global defaults
   * Acceptance: tenant settings API returns merged result
   * Cursor prompt:

     ```text
     Implement TenantService with createTenant, updateTenant, getTenantWithSettings that merges tenant_settings with system defaults.
     ```

10. Implement modules & menus service (dynamic menu builder)

    * Goal: CRUD for modules & submodules, build menu tree
    * Acceptance: menu builder returns nested array ordered by order\_index
    * Cursor prompt:

      ```text
      Implement ModuleService with createModule, createSubmodule, updateOrder, getMenuForUser(userId) that filters by role_permissions and is_active and returns nested menus.
      ```

11. Implement roles & role\_permissions service (permission matrix)

    * Goal: Role CRUD and assign permissions (matrix)
    * Acceptance: rolePermissions endpoints accept bulk assignment
    * Cursor prompt:

      ```text
      Implement RoleService and PermissionService to manage roles and role_permissions; include an endpoint to apply bulk permission updates and resolve effective permissions for a user.
      ```


* Insert test data if applicable.
* Run unit/integration/E2E tests for this step's feature.
* Fix any errors or failing tests.
* Re-run tests to confirm all pass before moving to next step.
* Push this code to GitHub after testing with title "Core Services & Repositories dynamic" as per the title.




**Authentication, Tokens & Device Management**

12. Implement auth service: login, logout, token generation, refresh rotation

    * Goal: Secure JWT RS256 tokens with access + refresh rotation
    * Acceptance: refresh token rotation on refresh endpoint
    * Cursor prompt:

      ```text
      Implement AuthService with login(email,password,tenantSlug), generateAccessToken(user), generateRefreshToken(user, deviceId), refreshToken(oldToken), logout(token) and rotate refresh tokens. Store refresh token records in DB with device info.
      ```

13. Implement login device tracking and revocation

    * Goal: create login\_devices records on login and support revoke endpoint
    * Acceptance: revoke sets is\_active=false and invalidates refresh tokens
    * Cursor prompt:

      ```text
      Implement LoginDeviceService: registerDevice(user, deviceInfo), listDevices(user), revokeDevice(deviceId) that marks device inactive and revokes related refresh tokens.
      ```

14. Implement reset password flow (reset\_tokens)

    * Goal: generate reset token, email stub, apply reset
    * Acceptance: reset token expires and is single-use
    * Cursor prompt:

      ```text
      Implement ResetTokenService that creates one-time tokens stored in DB and a controller that validates and resets user password.
      ```

* Push this code to GitHub after testing with title "Authentication, Tokens & Device Management dynamic" as per the title.

* Insert test data if applicable.
* Run unit/integration/E2E tests for this step's feature.
* Fix any errors or failing tests.
* Re-run tests to confirm all pass before moving to next step.


**Middleware & Guards**

15. Implement env loader, logger, error types, and global error handler

    * Goal: Centralized error types and handler middleware
    * Acceptance: errors returned in standard response shape
    * Cursor prompt:

      ```text
      Add error classes (AppError) and global errorHandler middleware that returns standardized JSON responses. Integrate pino logger and request-id middleware.
      ```

16. Implement authMiddleware (verify JWT + attach user)

    * Goal: Verify JWT, load user, attach req.user
    * Acceptance: protected routes return 401 without token
    * Cursor prompt:

      ```text
      Implement authMiddleware that verifies access token (RS256), loads user from DB and attaches req.user; on missing or invalid token return 401.
      ```

17. Implement tenantResolver middleware

    * Goal: Determine tenant context (token, header, or hostname)
    * Acceptance: req.tenant available for downstream
    * Cursor prompt:

      ```text
      Implement tenantResolver middleware which resolves tenant based on X-Tenant header or subdomain, validates tenant active flag and attaches tenant context to req.tenant.
      ```

18. Implement permissionGuard(moduleKey, action) middleware

    * Goal: Check user permissions for module & action, respect is\_superadmin
    * Acceptance: returns 403 when not allowed
    * Cursor prompt:

      ```text
      Implement permissionGuard(moduleKey:string,action:'create'|'read'|'update'|'delete') middleware that computes effective permissions for req.user and returns 403 if not permitted.
      ```



**API Routes & Controllers**

19. Scaffold route structure and route registration

    * Goal: central routes index and modular route files
    * Acceptance: routes mount under /api
    * Cursor prompt:

      ```text
      Create route files for auth, tenants, users, roles, modules, support, audit, tokens and an index route that mounts them under /api.
      ```

20. Implement Auth routes/controllers

    * Goal: login, refresh, logout, revoke-device, reset
    * Acceptance: passing integration tests
    * Cursor prompt:

      ```text
      Implement /api/auth endpoints with Zod validation and controllers that call AuthService. Include device registration on login.
      ```

21. Implement Users routes/controllers (list with filters)

    * Goal: list users with search, filters, pagination, sorting; create/update
    * Acceptance: support query params page/limit/search/sort/filter
    * Cursor prompt:

      ```text
      Implement /api/users endpoints with list (filters, search, pagination), create, update, soft-delete. Enforce tenant scope via tenantResolver and permissionGuard.
      ```

22. Implement Roles & Permissions routes/controllers

    * Goal: create roles, assign permissions matrix, list effective permissions for a user
    * Acceptance: role editor works end-to-end
    * Cursor prompt:

      ```text
      Implement /api/roles endpoints for CRUD and /api/roles/:id/permissions to get/set role permissions; implement endpoint to compute effective permissions for a user.
      ```

23. Implement Modules & Menus routes/controllers

    * Goal: modules CRUD, submodules CRUD, reorder API, menu tree API per user
    * Acceptance: menu API returns nested active menu items filtered by permissions
    * Cursor prompt:

      ```text
      Implement /api/modules and /api/modules/:id/submodules routes and /api/menu that returns the menu tree for the authenticated user.
      ```

24. Implement Support system routes/controllers

    * Goal: ticket CRUD, replies, attachments (S3 or local), file validation
    * Acceptance: attachments upload and replies stored and returned
    * Cursor prompt:

      ```text
      Implement /api/support/tickets, /api/support/tickets/:id/replies and attachments endpoints with file upload middleware, validation, and permission checks.
      ```

25. Implement Audit logs endpoints & integration

    * Goal: store audit entries from important actions and provide listing/export
    * Acceptance: audit entries created on user changes, role changes, tenant changes
    * Cursor prompt:

      ```text
      Implement audit logging helper and /api/audit endpoints; add hooks to services to create audit logs for create/update/delete actions.
      ```

?==>

**Pagination/Search/Filter Utilities**

26. Implement generic pagination & search utility

    * Goal: helper that converts query params into prisma/mysql queries supporting cursor and offset
    * Acceptance: used by all list endpoints
    * Cursor prompt:

      ```text
      Implement utils/pagination.ts and utils/search.ts to parse query params (page, limit, cursor, sortBy, sortOrder, q) and return prisma-compatible filter objects.
      ```

**Validation & Contracts**

27. Create Zod schemas for all endpoints

    * Goal: schema files for auth, user, tenant, role, module, support
    * Acceptance: validation middleware applied to routes
    * Cursor prompt:

      ```text
      Create Zod schemas in src/validation-schemas for auth (login/refresh/reset), user (create/update), tenant (create/update), role (create/update), module and support endpoints, and add middleware to validate requests.
      ```

**Testing: Unit & Integration**

28. Setup Jest config and test environment with test DB

    * Goal: jest, ts-jest and test DB connection
    * Acceptance: tests run with npm test
    * Cursor prompt:

      ```text
      Add Jest (ts-jest) configuration, setup test environment variables for MySQL test DB, and create a helper to run migrations and seed test data before tests.
      ```

29. Write unit tests for core services (users, auth, roles)

    * Goal: cover business logic, edge cases
    * Acceptance: high coverage for services
    * Cursor prompt:

      ```text
      Write Jest unit tests for UserService, AuthService and RoleService focusing on create, update, password hashing, and permission resolution logic.
      ```

30. Write integration tests for key APIs with Supertest

    * Goal: login + protected endpoints tests, refresh token flow
    * Acceptance: passing integration tests for auth and users
    * Cursor prompt:

      ```text
      Create Supertest integration tests for /api/auth and /api/users endpoints covering login, refresh, list users with filtering and permission behaviors.
      ```

Integration Testing - All utilities and schemas are tested and working
Route Implementation - Controllers can now use the validation middleware
Frontend Integration - Consistent API contracts for frontend development
===================

**Frontend – Setup & Core Features**

31. Scaffold Next.js + Tailwind + TypeScript frontend

    * Goal: repo with Next.js app, Tailwind, shadcn integration
    * Acceptance: `yarn dev` serves app
    * Cursor prompt:

      ```text
      Create a Next.js + TypeScript project named multi-tenant-frontend with TailwindCSS and shadcn UI preconfigured. Add eslint, prettier, and basic layout pages.
      ```

32. Add global state: Redux Toolkit + Redux Persist; TanStack Query + Axios

    * Goal: central store for auth and UI, queryClient for server state
    * Acceptance: auth slice and persisted store implemented
    * Cursor prompt:

      ```text
      Implement Redux Toolkit store with authSlice, tenantSlice, UI slice. Add redux-persist to persist auth. Setup Axios instance with interceptors and TanStack Query Provider.
      ```
??==>
33. Implement AuthGuard and route protection

    * Goal: HOC/component `AuthGuard` that checks Redux auth & permissions and redirects to login
    * Acceptance: protected pages inaccessible without login
    * Cursor prompt:

      ```text
      Implement an AuthGuard component that checks token presence and permissions from Redux/ReactQuery and protects pages; integrate with Next.js _app.tsx routing.
      ```

34. Implement skeleton loader & error boundary

    * Goal: reusable Skeleton component and ErrorBoundary wrapper
    * Acceptance: used around heavy components and pages
    * Cursor prompt:

      ```text
      Add a Skeleton component library and an ErrorBoundary component that logs errors and shows a friendly fallback UI. Wrap route-level components with ErrorBoundary.
      ```

35. Integrate shadcn components and build custom modal/confirmation modal

    * Goal: modal system using shadcn Dialog primitives; confirmation modal returns a promise
    * Acceptance: reusable modal with forms and confirm dialog
    * Cursor prompt:

      ```text
      Integrate shadcn UI and implement a ModalService with a reusable Dialog-based Modal and a ConfirmModal with promise resolution for confirmations.
      ```

36. Implement Notification library + global toasts

    * Goal: integrate `sonner` or `react-hot-toast` and a wrapper to use in services
    * Acceptance: global notifications for API responses and errors
    * Cursor prompt:

      ```text
      Add a NotificationService wrapped around sonner or react-hot-toast to show success/error/info notifications; integrate with Axios interceptors to show API errors.
      ```

37. Implement menu & sidebar based on modules and role permissions

    * Goal: sidebar reads menu from API and renders nested menus filtered by permissions
    * Acceptance: sidebar shows allowed modules only
    * Cursor prompt:

      ```text
      Implement Sidebar component that fetches /api/menu for authenticated user and renders nested menus; enforce collapsing and active route highlighting.
      ```

38. Implement tables, filters, pagination, search components connected to server

    * Goal: DataTable component that supports server-side pagination/sorting/search
    * Acceptance: reuse for users, tenants, support lists
    * Cursor prompt:

      ```text
      Create a DataTable component that accepts api endpoint and query params, uses TanStack Query for fetching, supports server-side pagination, sorting, filtering and search.
      ```

39. Implement forms (React Hook Form + Zod) and mutation flows

    * Goal: Create/edit forms with validation and optimistic UI updates
    * Acceptance: use forms for user/role/module CRUD
    * Cursor prompt:

      ```text
      Integrate React Hook Form with Zod resolver and create reusable form components. Implement mutation hooks using TanStack Query's useMutation for create/update flows.
      ```

40. Implement device management UI & revoke actions

    * Goal: show active devices and allow revoke; reflect counts vs tenant limits
    * Acceptance: revoke disables refresh token/server session
    * Cursor prompt:

      ```text
      Implement Devices page that lists login devices via /api/devices and allows revoke actions. Integrate with Auth state to force logout when current device revoked.
      ```

41. Implement support ticket UI with attachments & replies

    * Goal: ticket list, detail view with reply composer and attachments
    * Acceptance: attachments upload to API and show in thread
    * Cursor prompt:

      ```text
      Implement Support pages: ticket list (with filters), ticket detail with replies and attachment upload using a file input and progress bars.
      ```

42. Implement audit viewer UI and export

    * Goal: view audit logs with filters and export CSV
    * Acceptance: Audit export downloads filtered CSV
    * Cursor prompt:

      ```text
      Implement Audit page that supports filters by action, user, date range and an export button calling /api/audit/export.
      ```



**CI / Deployment / DevOps**

43. Dockerize backend & frontend with multi-stage builds

    * Goal: Dockerfiles for both apps and docker-compose for local dev
    * Acceptance: docker-compose up brings services up
    * Cursor prompt:

      ```text
      Create Dockerfiles for backend and frontend and a docker-compose.yml for local dev with mysql, adminer, backend, frontend, and run scripts for migrations and seeding.
      ```

44. Setup CI pipeline (GitHub Actions) to run lint, tests, migrations

    * Goal: pipeline runs on PRs and merges
    * Acceptance: fail on lint/test errors
    * Cursor prompt:

      ```text
      Add GitHub Actions workflow that runs install, lint, test (unit + integration), and runs prisma migrate:deploy on main branch. Include code scanning step.
      ```

45. Setup health checks & monitoring (Prometheus/Datadog compatibility)

    * Goal: readiness/liveness endpoints, metric exposition
    * Acceptance: GET /health returns status and DB check
    * Cursor prompt:

      ```text
      Add /health and /metrics endpoints; integrate pino for structured logs and expose basic metrics for monitoring.
      ```



**Polish & Documentation**

46. Generate OpenAPI (Swagger) docs for all endpoints

    * Goal: auto-generated docs and /docs route
    * Acceptance: OpenAPI JSON available
    * Cursor prompt:

      ```text
      Generate OpenAPI spec for all endpoints using tsdoc annotations or a generator and serve Swagger UI at /docs.
      ```

47. Write developer README with setup steps for local and test environments

    * Goal: clear README with env vars, migrations, seed, run and test commands
    * Acceptance: a new dev can bootstrap project in 30 minutes
    * Cursor prompt:

      ```text
      Create README.md with setup, env variable descriptions, migration steps, seed instructions, and commands for running tests and starting dev servers.
      ```

48. Create Postman / Insomnia collection & example tokens

    * Goal: easy manual testing
    * Acceptance: collection exported for team
    * Cursor prompt:

      ```text
      Export a Postman collection including example auth flows (login, refresh, protected endpoints) and add to repo under /docs/postman.
      ```

49. Security review and hardening checklist

    * Goal: run a checklist and address issues
    * Acceptance: checklist completed and documented
    * Cursor prompt:

      ```text
      Produce a security checklist including SQL injection review, XSS, CSRF, token storage, CORS policies, rate limits and ensure code implements recommendations.
      ```

50. Final polish: accessibility, performance, and QA

    * Goal: run Lighthouse, accessibility tests, fix major items
    * Acceptance: baseline performance and accessibility scores documented
    * Cursor prompt:

      ```text
      Run Lighthouse and axe-core audits and produce a report; fix high severity issues and document remaining items for future sprints.
      ```

* Push this code to GitHub after testing with title "Polish & Documentation dynamic" as per the title.

---

# 13. Cursor Prompts (Grouped)

Each of the 50 steps above includes a concise Cursor prompt. Use them in sequence. If you want a single file containing all the prompts ready to paste into Cursor, ask me and I'll export them as one combined prompt file.

---

# 14. Detailed Summary for Self‑Development (How to learn & implement this)

This section explains how a developer should approach learning and implementing the project. It contains recommended reading, sequence for learning, and tips for debugging and testing.

## Learning Path (4–8 weeks plan)

1. **Week 1 — TypeScript & Node.js Essentials**

   * Strongly type JavaScript, generics, union types
   * Express + TypeScript patterns
   * Async/await and error handling
2. **Week 2 — Databases & Prisma**

   * Relational modeling, indexing
   * Prisma schema & migrations
   * Transaction handling
3. **Week 3 — Security & Auth**

   * JWT (RS256 vs HS256), token rotation
   * OWASP top 10, secure headers
4. **Week 4 — Advanced Backend**

   * RBAC models, permission resolution algorithms
   * Pagination & cursor-based pagination
5. **Week 5 — Frontend Advanced**

   * Next.js + Tailwind + shadcn UI
   * TanStack Query + Redux Toolkit
   * Error boundaries, modals, skeletons
6. **Week 6 — Testing & DevOps**

   * Jest & Supertest, Playwright
   * Docker, CI, automated migrations

## Debugging tips

* Reproduce with integration tests first.
* Use consistent request-id logs for tracing.
* For auth issues, inspect JWT payload and expiry timestamps.

## Performance tips

* Index search fields, use pagination, limit joins.
* Cache frequently-read tenant settings in Redis if needed.
* Use connection pooling and prepared statements.

## How to iterate

* Start with minimal MVP: auth, tenants, users, roles, modules.
* Add support and audit logs next.
* Add full frontend features after stable APIs.
* Keep PRs small and test-driven.

---

# 15. Next Actions & How I Can Help

* I created step-by-step prompts for Cursor per step above (in each step block). If you want, I can:

  1. Export a single combined Cursor script that creates the entire backend scaffold.
  2. Generate the initial Prisma schema SQL and seed file now.
  3. Scaffold the frontend repo with AuthGuard, skeletons, modals and shadcn integration.

Tell me which of (1)-(3) you want next and I will run/produce the required artifacts.

---

*End of PRD*
