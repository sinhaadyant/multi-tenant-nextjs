# This file ensures the uploads directory is tracked by git
# The uploads directory is used for file uploads and should be empty in the repository
# Users should create their own .env file with appropriate UPLOAD_PATH configuration
